# تغييرات API - نظام إدارة المستشفى

## ملخص التغييرات الرئيسية

تم إجراء تحديثات شاملة على نظام الفواتير ليعمل **تلقائياً** بدلاً من الإدخال اليدوي:

1. ✅ **إنشاء الفاتورة تلقائياً** عند إنشاء زيارة
2. ✅ **إضافة رسوم الكشفية تلقائياً** إلى الفاتورة
3. ✅ **إضافة التحاليل تلقائياً** إلى الفاتورة عند طلبها
4. ✅ **إضافة الأشعة تلقائياً** إلى الفاتورة عند طلبها
5. ✅ **إضافة الأدوية تلقائياً** إلى الفاتورة عند كتابة الوصفة
6. ✅ **إضافة endpoints خاصة بالطبيب المسجل دخول**
7. ✅ **إضافة صلاحيات للممرضة**
8. ✅ **إصلاح مشاكل التحقق من صحة المعرفات**

---

## 1️⃣ التغييرات في نظام الفواتير (Invoices)

### السلوك الجديد:

#### 🔹 عند إنشاء زيارة (Create Visit)
- **تلقائياً**: يتم إنشاء فاتورة جديدة للمريض
- **تلقائياً**: يتم إضافة بند "الكشفية" للفاتورة (السعر الافتراضي: 50)
- **حالة الفاتورة**: PENDING

```json
// مثال: عند POST /visits
{
  "patientId": 1,
  "doctorId": 2,
  "diagnosis": "صداع",
  "chiefComplaint": "صداع مستمر"
}

// النتيجة:
// ✅ تم إنشاء الزيارة
// ✅ تم إنشاء فاتورة تلقائياً
// ✅ تم إضافة بند الكشفية (50) للفاتورة
```

#### 🔹 عند طلب تحاليل (Create Lab Request)
- **تلقائياً**: يتم إضافة جميع التحاليل المطلوبة إلى **فاتورة المريض المعلقة**
- يتم حساب السعر من جدول `LabTest`
- يتم تحديث `totalAmount` و `finalAmount` للفاتورة

```json
// مثال: عند POST /lab-requests
{
  "visitId": 10,
  "patientId": 1,
  "doctorId": 2,
  "items": [
    { "testId": 5, "notes": "صائم" },
    { "testId": 8, "notes": "" }
  ]
}

// النتيجة:
// ✅ تم إنشاء طلب التحليل
// ✅ تم إضافة التحاليل للفاتورة المعلقة تلقائياً
```

#### 🔹 عند طلب أشعة (Add Radiology Items)
- **تلقائياً**: يتم إضافة جميع فحوصات الأشعة إلى **فاتورة المريض المعلقة**
- يتم حساب السعر من جدول `RadiologyTest`

```json
// مثال: عند POST /radiology-requests/:id/items
{
  "testIds": [3, 7]
}

// النتيجة:
// ✅ تم إضافة فحوصات الأشعة للطلب
// ✅ تم إضافة الفحوصات للفاتورة المعلقة تلقائياً
```

#### 🔹 عند كتابة وصفة (Create Prescription)
- **تلقائياً**: يتم إضافة جميع الأدوية إلى **فاتورة المريض المعلقة**
- يتم حساب السعر من جدول `Medicine`

```json
// مثال: عند POST /prescriptions
{
  "visitId": 10,
  "patientId": 1,
  "doctorId": 2,
  "items": [
    { "medicineId": 12, "dosage": "500mg", "frequency": "مرتين يومياً", "duration": "5 أيام" }
  ]
}

// النتيجة:
// ✅ تم إنشاء الوصفة
// ✅ تم إضافة الأدوية للفاتورة المعلقة تلقائياً
```

---

## 2️⃣ التغييرات في endpoints الخاصة بالطبيب

### ✅ Endpoints جديدة خاصة بالطبيب المسجل دخول:

#### 🔹 GET /doctors/my-visits
**الوصف**: الحصول على زيارات الطبيب المسجل دخوله
**الصلاحية**: `DOCTOR` فقط
**التوكن**: مطلوب (JWT)
**المعاملات**: لا يوجد (يتم استخراج doctorId من التوكن تلقائياً)

```typescript
// Frontend Example
const response = await fetch('http://localhost:3001/doctors/my-visits', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
});
```

#### 🔹 POST /visits/my-visit
**الوصف**: إنشاء زيارة للطبيب المسجل دخوله
**الصلاحية**: `DOCTOR` فقط
**التوكن**: مطلوب (JWT)
**البيانات المطلوبة**: لا تحتاج `doctorId` (يتم استخراجه تلقائياً)

```json
// Frontend Example - Body
{
  "patientId": 1,
  "diagnosis": "نزلة برد",
  "chiefComplaint": "سعال"
}

// لاحظ: لا داعي لإرسال doctorId
```

#### 🔹 POST /lab-requests/my-request
**الوصف**: إنشاء طلب تحليل للطبيب المسجل دخوله
**الصلاحية**: `DOCTOR` فقط
**التوكن**: مطلوب (JWT)
**البيانات المطلوبة**: لا تحتاج `doctorId` و `patientId` (يتم استخراجهم تلقائياً من الزيارة)

```json
// Frontend Example - Body
{
  "visitId": 10,
  "items": [
    { "testId": 5, "notes": "صائم" }
  ]
}

// لاحظ: لا داعي لإرسال doctorId أو patientId
```

#### 🔹 GET /lab-requests/my-requests
**الوصف**: الحصول على طلبات تحاليل الطبيب المسجل دخوله
**الصلاحية**: `DOCTOR` فقط
**التوكن**: مطلوب (JWT)

#### 🔹 POST /radiology-requests/my-request
**الوصف**: إنشاء طلب أشعة للطبيب المسجل دخوله
**الصلاحية**: `DOCTOR` فقط
**التوكن**: مطلوب (JWT)

```json
// Frontend Example - Body
{
  "visitId": 10,
  "notes": "ملاحظات"
}
```

#### 🔹 GET /radiology-requests/my-requests
**الوصف**: الحصول على طلبات أشعة الطبيب المسجل دخوله
**الصلاحية**: `DOCTOR` فقط
**التوكن**: مطلوب (JWT)

---

## 3️⃣ التغييرات في صلاحيات الممرضة (NURSE)

### ✅ الصلاحيات الجديدة للممرضة:

#### 🔹 عرض جميع الأطباء
- **Endpoint**: `GET /doctors`
- **الصلاحيات**: `ADMIN, RECEPTIONIST, DOCTOR, NURSE`

#### 🔹 عرض طبيب واحد
- **Endpoint**: `GET /doctors/:id`
- **الصلاحيات**: `ADMIN, RECEPTIONIST, DOCTOR, NURSE`

#### 🔹 إنشاء مريض
- **Endpoint**: `POST /patients`
- **الصلاحيات**: `ADMIN, RECEPTIONIST, NURSE`

#### 🔹 تعديل بيانات مريض
- **Endpoint**: `PATCH /patients/:id`
- **الصلاحيات**: `ADMIN, RECEPTIONIST, NURSE`

#### 🔹 حذف مريض
- **Endpoint**: `DELETE /patients/:id`
- **الصلاحيات**: `ADMIN, RECEPTIONIST, NURSE`

#### 🔹 عرض المرضى
- **Endpoint**: `GET /patients`
- **الصلاحيات**: `ADMIN, RECEPTIONIST, DOCTOR, NURSE`

---

## 4️⃣ التغييرات في نظام الأدوية (Medicines)

### ✅ عرض الأدوية للطبيب:
- **Endpoint**: `GET /medicines`
- **الصلاحيات**: `ADMIN, PHARMACIST, DOCTOR, NURSE, RECEPTIONIST`
- **الوصف**: الآن يمكن للطبيب عرض قائمة الأدوية المتاحة

---

## 5️⃣ إصلاح مشاكل التحقق من صحة المعرفات

### المشكلة السابقة:
عند استدعاء endpoints مثل:
- `GET /doctors/my-visits`
- `GET /lab-requests/my-requests`

كان يتم تفسير `my-visits` و `my-requests` على أنها معرفات (IDs) مما يسبب خطأ "Invalid ID".

### ✅ الحل:
- تم نقل جميع endpoints المحددة (مثل `my-visits`) **قبل** endpoints المعرفات (`:id`)
- تم إضافة تحقق `isNaN()` في جميع دوال `findOne()`

---

## 6️⃣ سير العمل الجديد للمحاسب (CASHIER)

### السير السابق (قديم - تم إلغاؤه):
1. المحاسب ينشئ فاتورة يدوياً
2. المحاسب يضيف بنود الفاتورة يدوياً
3. المحاسب يحدد السعر يدوياً
4. المحاسب يدفع الفاتورة

### ✅ السير الجديد (تلقائي):
1. **الزيارة**: يتم إنشاء فاتورة تلقائياً مع رسوم الكشفية
2. **التحاليل**: تُضاف للفاتورة تلقائياً عند طلبها
3. **الأشعة**: تُضاف للفاتورة تلقائياً عند طلبها
4. **الأدوية**: تُضاف للفاتورة تلقائياً عند كتابة الوصفة
5. **المحاسب**: يقوم فقط بمراجعة الفاتورة ومعالجة الدفع

### Endpoints المحاسب:

#### 🔹 عرض جميع الفواتير
- **Endpoint**: `GET /invoices`
- **Query Parameters**: `?status=PENDING&patientId=1`

#### 🔹 عرض فاتورة محددة
- **Endpoint**: `GET /invoices/:id`

#### 🔹 معالجة الدفع
- **Endpoint**: `POST /invoices/:id/pay`
- **الوصف**: تغيير حالة الفاتورة من `PENDING` إلى `PAID`

```typescript
// Frontend Example
const payInvoice = async (invoiceId: number) => {
  const response = await fetch(`http://localhost:3001/invoices/${invoiceId}/pay`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
  return response.json();
};
```

---

## 7️⃣ تغييرات في التسجيل (Registration)

### ✅ عند تسجيل مستخدم بدور DOCTOR:
- **تلقائياً**: يتم إنشاء سجل في جدول `Doctor` مرتبط بحساب المستخدم
- **الفائدة**: الطبيب يستطيع استخدام endpoints الخاصة فوراً بعد التسجيل

```json
// POST /auth/register
{
  "fullName": "د. أحمد محمد",
  "email": "doctor@hospital.com",
  "password": "password123",
  "phone": "0599123456",
  "role": "DOCTOR",
  "specialization": "قلب"
}

// النتيجة:
// ✅ تم إنشاء حساب في جدول User
// ✅ تم إنشاء سجل في جدول Doctor تلقائياً
```

---

## 8️⃣ الأخطاء المحتملة ورسائلها

### ❌ "No pending invoice found for this patient"
**السبب**: لا توجد فاتورة معلقة للمريض
**الحل**: تأكد من وجود زيارة نشطة للمريض (الزيارة تنشئ فاتورة تلقائياً)

### ❌ "Invalid doctor ID" / "Invalid visit ID" / "Invalid lab request ID"
**السبب**: تم إرسال قيمة غير صحيحة للمعرف (ID)
**الحل**: تأكد من إرسال رقم صحيح، ليس نص مثل "my-visits"

### ❌ "Doctor profile not found for this user"
**السبب**: المستخدم المسجل دخوله لا يملك سجل في جدول Doctor
**الحل**: تأكد من أن المستخدم تم تسجيله بدور `DOCTOR`

### ❌ "This visit does not belong to the current doctor"
**السبب**: الطبيب يحاول طلب تحليل/أشعة لزيارة لا تخصه
**الحل**: تأكد من أن الزيارة تعود للطبيب المسجل دخوله

---

## 9️⃣ ملف DTOs الجديد

### الملفات الجديدة التي تم إنشاؤها:

1. **`src/auth/get-user.decorator.ts`**
   Decorator لاستخراج بيانات المستخدم من JWT Token

2. **`src/visits/dto/create-my-visit.dto.ts`**
   DTO خاص بإنشاء زيارة للطبيب (بدون حقل doctorId)

3. **`src/lab-requests/dto/create-my-lab-request.dto.ts`**
   DTO خاص بإنشاء طلب تحليل للطبيب (بدون doctorId و patientId)

4. **`src/radiology-requests/dto/create-my-radiology-request.dto.ts`**
   DTO خاص بإنشاء طلب أشعة للطبيب (بدون doctorId و patientId)

---

## 🔟 جدول مقارنة التغييرات

| الوظيفة | السلوك القديم | السلوك الجديد |
|---------|---------------|---------------|
| **إنشاء زيارة** | إنشاء زيارة فقط | إنشاء زيارة + فاتورة + بند الكشفية |
| **طلب تحليل** | إنشاء طلب فقط | إنشاء طلب + إضافة للفاتورة تلقائياً |
| **طلب أشعة** | إنشاء طلب فقط | إنشاء طلب + إضافة للفاتورة تلقائياً |
| **كتابة وصفة** | إنشاء وصفة فقط | إنشاء وصفة + إضافة الأدوية للفاتورة تلقائياً |
| **المحاسب** | إدخال يدوي كامل | مراجعة ودفع فقط |
| **الطبيب** | لا توجد endpoints خاصة | توجد endpoints خاصة بالمسجل دخوله |
| **الممرضة** | صلاحيات محدودة | صلاحيات كاملة على المرضى والأطباء |

---

## 📌 ملاحظات مهمة للفريق Frontend

1. ✅ **لا تحتاج لإرسال `doctorId`** عند استخدام endpoints "my-*"
2. ✅ **لا تحتاج لإنشاء فواتير يدوياً** - يتم إنشاؤها تلقائياً عند الزيارة
3. ✅ **لا تحتاج لإضافة بنود الفاتورة يدوياً** - يتم إضافتها تلقائياً
4. ✅ **تأكد من ترتيب Routes** في Frontend ليكون `/doctors/my-visits` قبل `/doctors/:id`
5. ✅ **الفواتير دائماً PENDING** حتى يقوم المحاسب بدفعها
6. ✅ **يجب وجود فاتورة معلقة** قبل طلب تحاليل/أشعة/أدوية (الزيارة تنشئها تلقائياً)

---

## 📂 الملفات المعدلة

### Backend Files Modified:
1. `src/auth/auth.service.ts` - إنشاء حساب Doctor تلقائياً
2. `src/auth/get-user.decorator.ts` - ملف جديد
3. `src/visits/visits.service.ts` - إنشاء فاتورة تلقائياً
4. `src/visits/visits.controller.ts` - endpoint جديد my-visit
5. `src/visits/dto/create-my-visit.dto.ts` - ملف جديد
6. `src/lab-requests/lab-requests.service.ts` - إضافة للفاتورة تلقائياً
7. `src/lab-requests/lab-requests.controller.ts` - endpoints جديدة
8. `src/lab-requests/dto/create-my-lab-request.dto.ts` - ملف جديد
9. `src/radiology-requests/radiology-requests.service.ts` - إضافة للفاتورة تلقائياً
10. `src/radiology-requests/radiology-requests.controller.ts` - endpoints جديدة
11. `src/radiology-requests/dto/create-my-radiology-request.dto.ts` - ملف جديد
12. `src/prescriptions/prescriptions.service.ts` - إضافة للفاتورة تلقائياً
13. `src/doctors/doctors.controller.ts` - إضافة صلاحيات NURSE
14. `src/doctors/doctors.service.ts` - دوال جديدة
15. `src/patients/patients.controller.ts` - إضافة صلاحيات NURSE
16. `src/medicines/medicines.controller.ts` - إضافة Guards

---

## ✅ الخلاصة

نظام الفواتير الآن **تلقائي بالكامل**:
1. الزيارة تنشئ فاتورة
2. الخدمات تُضاف للفاتورة تلقائياً
3. المحاسب يراجع ويدفع فقط
4. لا مزيد من الأخطاء اليدوية
5. سير عمل منطقي وسلس

---

**تاريخ التحديث**: 2026-01-04
**الإصدار**: 2.0
**حالة الإنتاج**: جاهز للاختبار
